Write a PR desc for the PR I'm about to submit. 

Read the code changes in the current branch (ignore `.wat`,`.hir`, `.masm`, `.lock` files). Run `git parent-branch` to print the name of the parent branch.

Write a PR description in `pr_desc.md` file, briefly explaining high-level changes introduced in this PR.
